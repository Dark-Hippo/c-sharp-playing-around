using System;

namespace SpeedRacer
{
    class Program
    {
        private const int WIDTH = 40;
        private const int HEIGHT = 40;

        private readonly Arena arena = new Arena(WIDTH, HEIGHT);

        static void Main(string[] args)
        {
            
        }
    }
}
